# Estructura del repositorio

> Generado por Guardian Suite (FACTORY-30). Fuente: TLM.

The root package is `co.quind.peajes`. Each bounded context is a sub-package (e.g., `co.quind.peajes.tollcollection`). Within each bounded context, the structure follows `domain/`, `application/`, and `infrastructure/` layers, with further sub-packages for specific concerns (e.g., `domain/model`, `application/usecase`, `infrastructure/web`).
