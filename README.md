# PEAJES-CO-01

Stack: **java** + Spring Boot 3.3.x + Spring WebFlux (build: maven 3.9.x) · tests: JUnit 5.10

## Setup local

```bash
    # stack no detectado — agregar comandos manualmente
```

## Guardian Suite

- `agentic context` — regenera contexto para tu AI coder
- `agentic check` — valida governance antes de commit
