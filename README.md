# PEAJES-CO

Stack: **java** + spring-boot (build: maven) · tests: junit

## Setup local

```bash
    ./mvnw clean install
    ./mvnw test
```

## Guardian Suite

- `agentic context` — regenera contexto para tu AI coder
- `agentic check` — valida governance antes de commit
